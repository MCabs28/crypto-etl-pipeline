version: 2

models:
  - name: stg_crypto_prices
    description: "Staging model containing cleaned cryptocurrency market data from the CoinGecko API."

    columns:
      - name: total_volume
        description: "Total trading volume in the last 24 hours."

        tests:
          - not_null
          - is_positive