version: 2

models:
  - name: stg_crypto_prices
    description: "Cleaned cryptocurrency price data."

    columns:
      - name: total_volume
        description: "24-hour trading volume."

        tests:
          - not_null